name:                yamltest
version:             0.1.0.0

dependencies:
- base >= 4.7 && < 5

executables:
  yamltest-exe:
    main:                Main.hs
    source-dirs:         app
    dependencies:
    - yaml
